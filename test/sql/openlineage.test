# name: test/sql/openlineage.test
# description: test openlineage extension
# group: [sql]

# Before we load the extension, this will fail
statement error
SELECT openlineage('Sam');
----
Catalog Error: Scalar Function with name openlineage does not exist!

# Require statement will ensure this test is run with this extension loaded
require openlineage

# Confirm the extension works
query I
SELECT openlineage('Sam');
----
Openlineage Sam 🐥

query I
SELECT openlineage_openssl_version('Michael') ILIKE 'Openlineage Michael, my linked OpenSSL version is OpenSSL%';
----
true
